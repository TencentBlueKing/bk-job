# V3.0.9.2 版本更新日志



- [ 新增 ] 定时任务新增查看 `执行记录` 的入口



- [ 优化 ] 消息通知的逻辑从 `业务+用户` 调整为按 `业务` 维度进行配置
- [ 优化 ] 标签的列表返回的顺序调整为按更新时间倒序
- [ 优化 ] 脚本管理的列表中 `去执行` 按钮是否可点击的逻辑判定优化
- [ 优化 ] 整站的搜索框进行了新一轮的交互体验优化（新增可搜索字段、人员字段可补全、上下键可操作）
- [ 优化 ] IP选择器的 `刷新状态` 按钮现在也可以对拓扑节点和动态分组起作用了
- [ 优化 ] 现在整站的IP复制出来的格式统一以 `换行符` 分隔
- [ 优化 ] 一些常规的视觉与交互体验的优化...



- [ 修复 ] 解决了定时任务引用的执行方案中包含数组变量时会导致启动失败的问题
- [ 修复 ] 解决了导入作业模板时选择保留ID选项时会触发导入失败的问题