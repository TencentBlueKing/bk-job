### 功能描述

启用或停用一个已有的定时任务。新建出来的定时任务默认处于停用状态，须调用本接口把状态置为启用后才会按计划触发；停用后定时任务会从调度中摘除，不再触发。

调用前须满足：目标定时任务已存在于指定的资源范围内，调用身份对该资源范围具备启停定时任务的操作权限。启用定时任务时还须对它所使用的执行方案具备启动权限。

### 请求参数

#### Body参数

| 字段 | 类型 | 必选 | 描述 |
|------|------|------|------|
| bk_scope_type | string | 是 | 资源范围类型。可选值：`biz`（业务）、`biz_set`（业务集） |
| bk_scope_id | string | 是 | 资源范围 ID，与 `bk_scope_type` 对应，表示业务 ID 或业务集 ID |
| id | long | 是 | 定时任务 ID，须为正整数 |
| status | int | 是 | 目标状态。可选值：1（启用）、0（停用） |

### 请求参数示例

- POST

```json
{
    "bk_scope_type": "biz",
    "bk_scope_id": "2",
    "id": 700001,
    "status": 1
}
```

### 返回结果示例

#### 失败示例

{% include '_generic_v4_error_resp.md.j2' %}

#### 成功示例

```json
# http status=200
{
    "data": {
        "id": 700001,
        "status": 1
    }
}
```

### 返回结果参数说明

{% include '_generic_v4_response.md.j2' %}

#### data

| 字段 | 类型 | 是否一定不为null | 描述 |
|------|------|------------|------|
| id | long | 是 | 定时任务 ID |
| status | int | 是 | 变更后的定时任务状态。可选值：1（已启用）、0（已停用） |
