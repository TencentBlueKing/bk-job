| 字段                 | 类型   | 是否一定存在 | 描述 |
|----------------------|--------|--------------|------|
| variable             | string | 否           | 引用模板中其他全局变量名称 |
| host_list            | array  | 否           | 静态主机列表 |
| dynamic_group_list   | array  | 否           | 动态分组列表 |
| topo_node_list       | array  | 否           | CMDB 拓扑节点列表 |

##### host_list[] 元素

| 字段          | 类型   | 是否一定存在 | 描述 |
|---------------|--------|--------------|------|
| bk_host_id    | long   | 否           | 主机在 CMDB 中的 ID |
| bk_cloud_id   | long   | 否           | 管控区域 ID |
| ip            | string | 否           | 主机 IP 地址 |

##### dynamic_group_list[] 元素

| 字段 | 类型   | 是否一定存在 | 描述 |
|------|--------|--------------|------|
| id   | string | 是           | 动态分组 ID |

##### topo_node_list[] 元素

| 字段       | 类型   | 是否一定存在 | 描述 |
|------------|--------|--------------|------|
| id         | long   | 是           | 拓扑节点 ID |
| node_type  | string | 是           | 拓扑节点类型。可选值：`module`（模块）、`set`（集群） |
