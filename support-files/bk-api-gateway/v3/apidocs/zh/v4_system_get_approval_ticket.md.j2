### 功能描述

获取一个审批任务的审批单据内容，供**审批渠道**建单并展示给审批人。本接口是应用态接口，没有用户身份。

调用方限制：

- 调用方必须是该审批任务**指派的审批渠道对应的应用**（渠道 appCode 与任务记录严格相等），其余应用即使拥有本接口权限也一律按「审批任务不存在」处理——单据中含脚本明文，不能让任何应用枚举他人的单据内容。
- 必须透传租户请求头 `X-Bk-Tenant-Id`，取值为发起审批接口返回的 `tenant_id`。多租户环境下缺失该请求头会在网关层直接返回 401；服务端还会将其与审批任务的租户严格比对，不一致同样按「审批任务不存在」处理。

单据正文 `approval_content` 是一份**由作业平台渲染好的 Markdown**，渠道直接展示即可，无需自行拼装。正文依次包含操作概要表格、各执行步骤的明细表格、脚本内容代码块与原始参数 JSON 代码块，其中概要部分摊开的是**解析后的实际影响面**（真实的目标机清单、执行账号、生效的默认值等），让审批人看清「要在哪些机器上执行什么」。敏感字段的值只是占位符，不含真实值；**脚本内容例外，原样展示**，否则审批人无从判断风险。

{% include '_generic_v4_approval_flow.md.j2' %}

### 请求参数

#### Header参数

| 字段 | 类型 | 必选 | 描述 |
|------|------|------|------|
| X-Bk-Tenant-Id | string | 是 | 审批任务所属租户 ID，取发起审批接口返回的 `tenant_id`；与审批任务的租户不一致时按审批任务不存在处理 |

#### Query参数

| 字段 | 类型 | 必选 | 描述 |
|------|------|------|------|
| approval_task_id | string | 是 | 审批任务 ID，由发起审批的接口返回并由调用方交给审批渠道 |

### 请求参数示例

- GET

```
/api/v4/system/get_approval_ticket?approval_task_id=5f9c1d1a3f2b4a1e9f2d7c1b2e3a4d5f
```

### 返回结果示例

#### 失败示例

{% include '_generic_v4_error_resp.md.j2' %}

#### 成功示例

```json
# http status=200
{
    "data": {
        "approval_task_id": "5f9c1d1a3f2b4a1e9f2d7c1b2e3a4d5f",
        "title": "快速执行脚本 - 蓝鲸 - 37个执行对象",
        "risk_level": "HIGH",
        "operation_type": "FAST_EXECUTE_SCRIPT",
        "bk_scope_type": "biz",
        "bk_scope_id": "2",
        "creator": "admin",
        "expire_at": 1786694400000,
        "approval_content": "# 快速执行脚本 - 蓝鲸 - 37个执行对象\n\n## 操作概要\n\n| 项目 | 内容 |\n| --- | --- |\n| 操作类型 | 快速执行脚本 |\n| 资源范围 | 蓝鲸(biz:2) |\n| 发起人 | admin |\n| **风险等级** | **高** |\n| 执行对象数量 | 37 |\n| **高危语句命中** | **已命中高危语句规则，请重点确认** |\n| **目标包含动态分组** | **目标包含动态分组或拓扑节点，实际执行的机器在放行时重新解析确定，可能与此处不同** |\n| 默认生效 超时时间 | 7200 |\n\n## 步骤1\n\n| 项目 | 内容 |\n| --- | --- |\n| 步骤名称 | 执行脚本 |\n| **执行账号** | **root** |\n| 脚本名称 | check_disk.sh |\n| 执行对象数量 | 37 |\n| 执行对象 | 127.0.0.1, 127.0.0.2 等另外35个 |\n\n## 脚本内容\n\n`script_content`\n\n```\n#!/bin/bash\nrm -rf /tmp/cache\n```\n\n## 原始参数\n\n```json\n{\n  \"script_content\" : \"见「脚本内容」章节\",\n  \"script_param\" : \"******\"\n}\n```\n"
    }
}
```

### 返回结果参数说明

{% include '_generic_v4_response.md.j2' %}

#### data

| 字段 | 类型 | 描述 |
|------|------|------|
| approval_task_id | string | 审批任务 ID |
| title | string | 单据标题，形如「快速执行脚本 - 某业务 - 37个执行对象」 |
| risk_level | string | 风险等级，由高危命中与目标规模推导，**仅作为给审批人的提示强度，不参与放行校验**。可选值：`HIGH`（高危，命中高危脚本规则或使用 root 等高危账号）、`MEDIUM`（中等，执行目标规模较大，或目标含动态分组、拓扑节点）、`LOW`（低） |
| operation_type | string | 操作类型。可选值：`FAST_EXECUTE_SCRIPT`（快速执行脚本）、`FAST_TRANSFER_FILE`（快速分发文件）、`EXECUTE_JOB_PLAN`（启动执行方案）、`CREATE_JOB_PLAN`（创建执行方案）、`SAVE_CRON`（保存定时任务）、`UPDATE_CRON_STATUS`（启停定时任务） |
| bk_scope_type | string | 资源范围类型。可选值：`biz`（业务）、`biz_set`（业务集） |
| bk_scope_id | string | 资源范围 ID，与 `bk_scope_type` 对应，表示业务 ID 或业务集 ID |
| creator | string | 审批任务发起人。审批人必须为发起人本人，渠道需据此校验 |
| expire_at | long | 审批任务过期时刻，Unix 时间戳，单位毫秒；过期后不可再放行 |
| approval_content | string | 单据正文，Markdown 格式，由作业平台渲染完成，渠道直接展示即可。依次包含操作概要表格（操作类型、资源范围、发起人、风险等级、执行对象清单、生效的默认值等）、各执行步骤的明细表格、脚本内容代码块、原始参数 JSON 代码块。需显著标注的高危项（root 等高危账号、命中高危语句规则、动态分组目标）在正文中以 Markdown 加粗呈现。密码类敏感字段的值仅为占位符，不含真实值，**脚本内容例外、原样展示**；目标含动态分组或拓扑节点时正文中会给出「实际执行的机器在放行时重新解析确定」的提示 |
