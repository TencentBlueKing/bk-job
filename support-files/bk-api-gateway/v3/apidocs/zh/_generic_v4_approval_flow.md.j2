### 审批流程说明

「带审批」类接口**不会立即执行操作**，而是先创建一个审批任务。完整流程如下：

1. 调用方调用发起接口。作业平台对参数做一次完整预检（权限、目标解析、高危规则等），预检通过后返回 `approval_task_id`；此时操作**尚未执行**。
2. 调用方把 `approval_task_id` 交给审批渠道，由渠道创建审批单据。
3. 审批渠道凭 `approval_task_id` 调用取单接口 `/api/v4/system/get_approval_ticket`，取回单据内容并展示给审批人。
4. 审批人在渠道内完成审批后，调用方用 `approval_task_id` 与渠道单据 ID `approval_ticket_id` 调用 `/api/v4/refresh_approval_task` 推进任务。
5. 作业平台**自行向审批渠道回查审批结论**：通过则执行原操作，并把执行结果放在 `execute_result` 中返回；被驳回则任务转为 `REJECTED`。

流程中的硬性约束：

- **审批结论只能由作业平台回查得出。** `refresh_approval_task` 只接受 `approval_task_id` 与 `approval_ticket_id` 两个 ID，不接受任何审批结论字段，调用方无法声明审批是否通过。
- **审批人必须是审批任务的发起人本人**，由他人审批不予放行。
- 审批任务有存活期，默认 **8 小时**（部署时可由服务端调整），准确的过期时刻见响应中的 `expire_at`。过期后不可再放行，只能重新发起。
- **一次审批只能放行一次。** 任务被放行后即被消费，重复调用 `refresh_approval_task` 只会返回同一份既有结果，不会重复执行。
- 审批任务不会自动放行，也不会因为渠道故障而放行；回查失败一律拒绝本次放行、任务保持待审批。
- 审批单据中的敏感内容（密文类全局变量、加密后的主机账号密码、被声明为敏感的脚本参数）只以占位符呈现；**脚本内容例外，原样展示**，否则审批人无从判断风险。
