### 功能描述

获取一个审批任务的审批内容，供**审批渠道**建单并展示给审批人。本接口是**应用态接口**。

调用方限制（任一不满足都按「审批任务不存在」处理，不区分「不存在」与「无权访问」）：

- 调用方必须是该审批任务**指派的审批渠道对应的应用**；
- 请求头 `X-Bk-Username` 传入的用户必须是该审批任务的**发起人本人**；
- 该用户所属租户必须与审批任务的租户一致。

因此渠道侧必须以**当前审批人**的身份取内容，不能固定传某个账号。本接口**不支持自助申请权限**，需由作业平台管理员授权后方可调用。

审批内容 `approval_content` 是一份**由作业平台渲染好的 Markdown**，渠道直接展示即可，无需自行拼装。正文依次包含一级标题、操作概要表格、脚本内容代码块与原始参数 JSON 代码块，标题、发起人、风险等级等信息都已渲染在其中，因此返回体不再单独给出结构化字段。概要部分给出的是**预检解析后的实际影响面**（风险等级、执行账号、脚本参数、执行对象、文件分发场景下的源文件与目标路径、传输模式、生效的默认值、高危与动态目标提示等），逐步骤的明细不在正文中展开，避免单据过长导致审批人直接划到底放行。敏感字段的值只是占位符，不含真实值；**脚本内容例外，原样展示**，否则审批人无从判断风险。

风险等级仅供审批人参考，不参与放行校验，判定规则如下：

| 风险等级 | 判定条件 |
|------|------|
| 高危 | 命中高危语句规则，或影响主机数超过 100 |
| 中危 | 影响主机数超过 10 |
| 低危 | 其余情况 |

其中「影响主机数」取**执行对象总数**与**各主机类全局变量台数合计**的较大值：创建执行方案、保存定时任务没有执行对象总数，影响面全在变量里，不计入的话一个指向上千台机器的方案会被判成低风险；而启动执行方案时主机变量已被解析进步骤目标、计入了执行对象总数，取较大值而非相加，可避免同一批机器被算两次。

{% include '_generic_v4_approval_flow.md.j2' %}

### 请求参数

#### Header参数

| 字段 | 类型 | 必选 | 描述 |
|------|------|------|------|
| X-Bk-Username | string | 是 | 取内容时的用户，必须传当前审批人 |

#### Query参数

| 字段 | 类型 | 必选 | 描述 |
|------|------|------|------|
| approval_task_id | string | 是 | 审批任务 ID，由发起审批的接口返回并由调用方交给审批渠道 |

### 请求参数示例

- GET

```
/api/v4/system/get_approval_content?approval_task_id=5f9c1d1a3f2b4a1e9f2d7c1b2e3a4d5f
```

### 返回结果示例

#### 失败示例

{% include '_generic_v4_error_resp.md.j2' %}

#### 成功示例

```json
# http status=200
{
    "data": {
        "approval_task_id": "5f9c1d1a3f2b4a1e9f2d7c1b2e3a4d5f",
        "expire_at": 1786694400000,
        "approval_content": "# 快速执行脚本 - 蓝鲸 - 37个执行对象\n\n## 操作概要\n\n| 项目 | 内容 |\n| --- | --- |\n| 操作类型 | 快速执行脚本 |\n| 资源范围 | 蓝鲸(biz:2) |\n| 发起人 | admin |\n| **风险等级** | **高危** |\n| 操作对象 | 清理磁盘缓存 |\n| 执行账号 | root |\n| 脚本参数 | --env=prod --force |\n| 执行对象 | 共37个，数量过多不逐个展示 |\n| **高危语句检测** | **已命中高危语句规则，请重点确认** |\n| **动态目标提示** | **目标包含动态分组或拓扑节点，实际执行的机器在放行时重新解析确定，可能与此处不同** |\n| [默认] 超时时间 | 7200s |\n\n## 脚本内容\n\n```shell\n#!/bin/bash\nrm -rf /tmp/cache\n```\n\n## 原始参数\n\n```json\n{\n  \"script_content\" : \"见「脚本内容」章节\",\n  \"script_param\" : \"******\"\n}\n```\n"
    }
}
```

#### 文件分发场景的操作概要示例

文件分发场景的概要中会多出源文件、目标路径、目标文件名（未指定时不出现）与传输模式四行，取值都来自预检解析结果。源文件按「账号@源机器: 文件路径」给出，源机器不超过 10 台时逐台列出（云区域ID:IP）、超过只报台数，本地上传的文件没有源机器与源账号，标注为「[本地文件]」。传输模式是**实际生效模式**，调用时未传 `transfer_mode` 才会标注「[默认]」，显式传入时不标注。带「[默认]」前缀的行（含按默认生效的传输模式）统一排在概要表格的最后，普通字段行在前。渲染出的表格形如：

```markdown
| 项目 | 内容 |
| --- | --- |
| 操作类型 | 快速分发文件 |
| 资源范围 | 蓝鲸(biz:2) |
| 发起人 | admin |
| 风险等级 | 低 |
| 操作对象 | 分发安装包 |
| 执行账号 | root |
| 源文件 | root@0:127.0.0.9: /pkg/app.tar.gz,/pkg/conf.yaml; [本地文件] /tmp/20260901/db.sql |
| 目标路径 | /data/app/ |
| 传输模式 | 保险模式（同名文件按源IP分目录存放，目标路径不存在时自动创建） |
| 执行对象 | 3个：0:127.0.0.1; 0:127.0.0.2; 0:127.0.0.3 |
| [默认] 超时时间 | 7200s |
```

执行方案带文件分发步骤时同样如此，多个步骤的源文件与目标路径去重后合并到同一行；单行最多列 5 条，超出的不再逐条展开，只补一句总数。启动执行方案的概要中还会给出全局变量（见下文「全局变量章节示例」）：

```markdown
| 项目 | 内容 |
| --- | --- |
| 操作类型 | 启动执行方案 |
| 资源范围 | 蓝鲸(biz:2) |
| 发起人 | admin |
| 风险等级 | 中等 |
| 操作对象 | 发布流程 |
| 执行账号 | root |
| 源文件 | root@0:127.0.0.9: /pkg/app-0.tar.gz; root@0:127.0.0.9: /pkg/app-1.tar.gz; root@0:127.0.0.9: /pkg/app-2.tar.gz; root@0:127.0.0.9: /pkg/app-3.tar.gz; root@0:127.0.0.9: /pkg/app-4.tar.gz; 其余已省略（共7项） |
| 目标路径 | /data/app/ |
| 传输模式 | 强制模式（同名文件覆盖，目标路径不存在时自动创建） |
| 执行方案ID | 1000 |
| 全局变量 | 共4项，见下方同名章节 |
| 执行对象 | 共12个，数量过多不逐个展示 |
```

#### 创建执行方案场景的操作概要示例

启用的步骤按名称逐行给出。**表格里只报条数，步骤名在概要表格之后的同名章节中以列表逐行列出**（表格单元格容不下换行，正文也不使用 `<br>` 等 HTML 标签）；启用的是全部模板步骤时，标签与章节标题都注明「（全部）」：

```markdown
| 项目 | 内容 |
| --- | --- |
| 操作类型 | 创建执行方案 |
| 资源范围 | 蓝鲸(biz:2) |
| 发起人 | admin |
| 风险等级 | 低 |
| 操作对象 | 发布流程-灰度 |
| 作业模板ID | 1000 |
| 启用的步骤 | 共3项，见下方同名章节 |
| 全局变量 | 共4项，见下方同名章节 |

## 启用的步骤

- 停止服务
- 分发安装包
- 启动服务
```

#### 全局变量章节示例

启动执行方案、创建执行方案与保存定时任务的正文中会多出「全局变量」章节，把**生效的全部变量**逐行列出，包括本次请求没指定、沿用作业模板/执行方案现有配置的那些——沿用来的取值一样会被执行，只列本次改动的等于让审批人蒙着眼放行。沿用现值的行加「[沿用现值]」前缀并统一沉底。

主机类变量的取值随台数变化：不超过 10 台时连同主机清单（云区域ID:IP）一起给出，超过 10 台只报总数；动态分组与拓扑节点算不出台数，只报个数（实际主机在放行时才解析，概要表格里另有一行动态目标提示）。密文类变量只出占位符，不含真实值。

```markdown
## 全局变量

| 变量 | 类型 | 取值 |
| --- | --- | --- |
| version | 字符串 | v1.2.3 |
| target_hosts | 主机列表 | 3台：0:127.0.0.1; 0:127.0.0.2; 0:127.0.0.3 |
| db_password | 密文 | ****** |
| [沿用现值] batch_hosts | 主机列表 | 共236台主机，数量过多不逐台展示 |
```

### 返回结果参数说明

{% include '_generic_v4_response.md.j2' %}

#### data

| 字段 | 类型 | 描述 |
|------|------|------|
| approval_task_id | string | 审批任务 ID |
| expire_at | long | 审批任务过期时刻，Unix 时间戳，单位毫秒；过期后不可再放行 |
| approval_content | string | 审批内容，Markdown 格式，由作业平台渲染完成，渠道直接展示即可。依次包含一级标题（形如「快速执行脚本 - 某业务 - 37个执行对象」）、操作概要表格（操作类型、资源范围、发起人、风险等级、操作对象、执行账号、脚本参数、执行对象、源文件、目标路径、目标文件名、传输模式、生效的默认值等）、需逐行展示的字段章节（如「启用的步骤」）、脚本内容代码块（代码块带脚本语言标记，如 shell、python，便于渠道侧高亮）、原始参数 JSON 代码块。**正文只使用标题、表格、列表、代码块等基础 Markdown 语法，不含 `<br>` 等内联 HTML**，渠道侧无需支持 HTML 即可正确展示。执行账号取各步骤解析出的账号别名去重合并；脚本参数只在存在脚本步骤且调用方传了参数时出现，同样取各步骤解析结果去重合并，调用方声明为敏感参数（`is_param_sensitive` 为 true）时只出占位符、不含真实值；执行对象总数不超过 10 个时连同清单一并给出（主机为云区域ID:IP，容器为容器名），超过 10 个只报总数；源文件、目标路径、目标文件名与传输模式只在存在文件分发步骤时出现（执行方案含文件分发步骤时同样给出），均取各步骤预检解析出的结果去重合并，其中源文件按「账号@源机器: 文件路径」的形式给出，源机器不超过 10 台时逐台列出（云区域ID:IP）、超过 10 台只报台数，本地上传的文件没有源机器与源账号、标注为「[本地文件]」，目标文件名仅在调用方指定了重命名时才出现，传输模式为**实际生效模式**（严谨模式 / 强制模式 / 保险模式）并说明同名文件与不存在的目标路径如何处置、未传 `transfer_mode` 而按默认（强制模式）生效时标注「[默认]」；带「[默认]」前缀的行统一排在概要表格的最后，普通字段行在前；这些汇总行单行最多列 5 条，超出的不再逐条展开、只在末尾补一句「其余已省略（共 N 项）」；保存定时任务的「操作」行取值为「创建」/「更新」，随请求语言切换；定时任务的「定时规则」行按调用方提交的 UNIX 格式（如 `30 10 8 * *`）展示，而非转换后落库的 Quartz 格式；创建执行方案的「启用的步骤」给出步骤名称而非步骤 ID，概要表格中只报条数（「共 N 项，见下方同名章节」），步骤名在概要表格之后的同名章节中以 Markdown 无序列表逐行列出、不做条数截断，启用的是全部模板步骤时标签与章节标题都注明为「启用的步骤（全部）」；启动执行方案、创建执行方案与保存定时任务的正文中还有「全局变量」章节，以「变量 / 类型 / 取值」三列表格列出**生效的全部变量**（含本次请求未指定、沿用作业模板或执行方案现有配置的那些，这类行加「[沿用现值]」前缀并统一沉底），概要表格中同样只报条数；主机类变量不超过 10 台时连同主机清单（云区域ID:IP）一起给出、超过 10 台只报总数，动态分组与拓扑节点算不出台数、只报个数，密文类变量只出占位符；逐步骤的明细（步骤名称、脚本名称、目标机清单等）不在正文中展开，以免单据过长使审批人直接划到底放行。需显著标注的高危项（命中高危语句规则、动态分组目标）在正文中以 Markdown 加粗呈现。原始参数中未传的字段不会展示，只列出调用方实际传入的内容。密码类敏感字段的值仅为占位符，不含真实值，**脚本内容例外、原样展示**；目标含动态分组或拓扑节点时正文中会给出「实际执行的机器在放行时重新解析确定」的提示 |
