### 功能描述

获取一个审批任务的审批内容，供**审批渠道**建单并展示给审批人。本接口是**应用态接口**。

调用方限制（任一不满足都按「审批任务不存在」处理，不区分「不存在」与「无权访问」）：

- 调用方必须是该审批任务**指派的审批渠道对应的应用**；
- 请求头 `X-Bk-Username` 传入的用户必须是该审批任务的**发起人本人**；
- 该用户所属租户必须与审批任务的租户一致。

因此渠道侧必须以**当前审批人**的身份取内容，不能固定传某个账号。本接口**不支持自助申请权限**，需由作业平台管理员授权后方可调用。

审批内容 `approval_content` 是一份**由作业平台渲染好的 Markdown**，渠道直接展示即可，无需自行拼装。正文依次包含一级标题、操作概要表格、脚本内容代码块与原始参数 JSON 代码块，标题、发起人、风险等级等信息都已渲染在其中，因此返回体不再单独给出结构化字段。概要部分给出的是**预检解析后的实际影响面**（风险等级、执行账号、文件分发场景下的源文件与目标路径、传输模式、执行对象总数、生效的默认值、高危与动态目标提示等），逐步骤的明细不在正文中展开，避免单据过长导致审批人直接划到底放行。敏感字段的值只是占位符，不含真实值；**脚本内容例外，原样展示**，否则审批人无从判断风险。

风险等级仅供审批人参考，不参与放行校验，判定规则如下：

| 风险等级 | 判定条件 |
|------|------|
| 高危 | 命中高危语句规则，或执行对象数超过 100 |
| 中危 | 执行对象数超过 10 |
| 低危 | 其余情况 |

{% include '_generic_v4_approval_flow.md.j2' %}

### 请求参数

#### Header参数

| 字段 | 类型 | 必选 | 描述 |
|------|------|------|------|
| X-Bk-Username | string | 是 | 取内容时的用户，必须传当前审批人 |

#### Query参数

| 字段 | 类型 | 必选 | 描述 |
|------|------|------|------|
| approval_task_id | string | 是 | 审批任务 ID，由发起审批的接口返回并由调用方交给审批渠道 |

### 请求参数示例

- GET

```
/api/v4/system/get_approval_content?approval_task_id=5f9c1d1a3f2b4a1e9f2d7c1b2e3a4d5f
```

### 返回结果示例

#### 失败示例

{% include '_generic_v4_error_resp.md.j2' %}

#### 成功示例

```json
# http status=200
{
    "data": {
        "approval_task_id": "5f9c1d1a3f2b4a1e9f2d7c1b2e3a4d5f",
        "expire_at": 1786694400000,
        "approval_content": "# 快速执行脚本 - 蓝鲸 - 37个执行对象\n\n## 操作概要\n\n| 项目 | 内容 |\n| --- | --- |\n| 操作类型 | 快速执行脚本 |\n| 资源范围 | 蓝鲸(biz:2) |\n| 发起人 | admin |\n| **风险等级** | **高危** |\n| 操作对象 | 清理磁盘缓存 |\n| 执行账号 | root |\n| 执行对象总数 | 37 |\n| **高危语句检测** | **已命中高危语句规则，请重点确认** |\n| **动态目标提示** | **目标包含动态分组或拓扑节点，实际执行的机器在放行时重新解析确定，可能与此处不同** |\n| [默认] 超时时间 | 7200s |\n\n## 脚本内容\n\n```shell\n#!/bin/bash\nrm -rf /tmp/cache\n```\n\n## 原始参数\n\n```json\n{\n  \"script_content\" : \"见「脚本内容」章节\",\n  \"script_param\" : \"******\"\n}\n```\n"
    }
}
```

#### 文件分发场景的操作概要示例

文件分发场景的概要中会多出源文件、目标路径、目标文件名（未指定时不出现）与传输模式四行，取值都来自预检解析结果。传输模式是**实际生效模式**，调用时未传 `transfer_mode` 才会标注「[默认]」，显式传入时不标注。带「[默认]」前缀的行（含按默认生效的传输模式）统一排在概要表格的最后，普通字段行在前。渲染出的表格形如：

```markdown
| 项目 | 内容 |
| --- | --- |
| 操作类型 | 快速分发文件 |
| 资源范围 | 蓝鲸(biz:2) |
| 发起人 | admin |
| 风险等级 | 低 |
| 操作对象 | 分发安装包 |
| 执行账号 | root |
| 源文件 | root: /pkg/app.tar.gz,/pkg/conf.yaml; mysql: /pkg/db.sql |
| 目标路径 | /data/app/ |
| 传输模式 | 保险模式（同名文件按源IP分目录存放，目标路径不存在时自动创建） |
| 执行对象总数 | 3 |
| [默认] 超时时间 | 7200s |
```

执行方案带文件分发步骤时同样如此，多个步骤的源文件与目标路径去重后合并到同一行；单行最多列 5 条，超出的不再逐条展开，只补一句总数：

```markdown
| 项目 | 内容 |
| --- | --- |
| 操作类型 | 启动执行方案 |
| 资源范围 | 蓝鲸(biz:2) |
| 发起人 | admin |
| 风险等级 | 中等 |
| 操作对象 | 发布流程 |
| 执行账号 | root |
| 源文件 | root: /pkg/app-0.tar.gz; root: /pkg/app-1.tar.gz; root: /pkg/app-2.tar.gz; root: /pkg/app-3.tar.gz; root: /pkg/app-4.tar.gz; 其余已省略（共7项） |
| 目标路径 | /data/app/ |
| 传输模式 | 强制模式（同名文件覆盖，目标路径不存在时自动创建） |
| 执行方案ID | 1000 |
| 执行对象总数 | 12 |
```

### 返回结果参数说明

{% include '_generic_v4_response.md.j2' %}

#### data

| 字段 | 类型 | 描述 |
|------|------|------|
| approval_task_id | string | 审批任务 ID |
| expire_at | long | 审批任务过期时刻，Unix 时间戳，单位毫秒；过期后不可再放行 |
| approval_content | string | 审批内容，Markdown 格式，由作业平台渲染完成，渠道直接展示即可。依次包含一级标题（形如「快速执行脚本 - 某业务 - 37个执行对象」）、操作概要表格（操作类型、资源范围、发起人、风险等级、操作对象、执行账号、源文件、目标路径、目标文件名、传输模式、执行对象总数、生效的默认值等）、脚本内容代码块（代码块带脚本语言标记，如 shell、python，便于渠道侧高亮）、原始参数 JSON 代码块。执行账号取各步骤解析出的账号别名去重合并；源文件、目标路径、目标文件名与传输模式只在存在文件分发步骤时出现（执行方案含文件分发步骤时同样给出），均取各步骤预检解析出的结果去重合并，其中源文件按「执行账号: 文件路径」的形式给出、不含源机器清单（需确认从哪台机器取时，快速分发文件可看正文中的原始参数，执行方案可查该执行方案的步骤定义），目标文件名仅在调用方指定了重命名时才出现，传输模式为**实际生效模式**（严谨模式 / 强制模式 / 保险模式）并说明同名文件与不存在的目标路径如何处置、未传 `transfer_mode` 而按默认（强制模式）生效时标注「[默认]」；带「[默认]」前缀的行统一排在概要表格的最后，普通字段行在前；这些汇总行单行最多列 5 条，超出的不再逐条展开、只在末尾补一句「其余已省略（共 N 项）」；保存定时任务的「操作」行取值为「创建」/「更新」，随请求语言切换；逐步骤的明细（步骤名称、脚本名称、目标机清单等）不在正文中展开，以免单据过长使审批人直接划到底放行。需显著标注的高危项（命中高危语句规则、动态分组目标）在正文中以 Markdown 加粗呈现。原始参数中未传的字段不会展示，只列出调用方实际传入的内容。密码类敏感字段的值仅为占位符，不含真实值，**脚本内容例外、原样展示**；目标含动态分组或拓扑节点时正文中会给出「实际执行的机器在放行时重新解析确定」的提示 |
