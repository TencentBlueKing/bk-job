apiVersion: v1
kind: Secret
metadata:
  name: {{ include "common.names.fullname" . }}-actuator-basic-auth
data:
  user: {{ .Values.job.security.actuator.user.name }}
  password: {{ .Values.job.security.actuator.user.password }}
type: Opaque
