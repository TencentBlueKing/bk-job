apiVersion: v1
kind: Service
metadata:
  name: {{ include "bk-job.fullname" . }}
  labels:
    {{- include "bk-job.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bk-job.selectorLabels" . | nindent 4 }}
